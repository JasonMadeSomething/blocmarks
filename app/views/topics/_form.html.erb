<%= form_for topic do |f| %>
    <%= form_group_tag(topic.errors[:title]) do %>
        <%= f.label :title %>
        <%= f.text_field :title, class: "form-control", placeholder: "Enter topic title" %>
    <% end %>
    <div class="form-group">
        <%= f.submit "Add Topic", class: "btn btn-success" %>
    </div>
<% end %>